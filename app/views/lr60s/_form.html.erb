<h3>โครงการกิจกรรม จำนวนกลุ่มเป้าหมาย ประมาณการและประเมินทรัพยากรบุคคลที่ต้องใช้ในการดำเนินงานโครงการปี 2554</h3>
<%= form_for(@lr60) do |f| %>
  <% if @lr60.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lr60.errors.count, "error") %> prohibited this lr60 from being saved:</h2>

      <ul>
      <% @lr60.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <!--
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>-->
  <%= hidden_field_tag 'lr60[user_id]', current_user.id %>
  <div class="field">
    <%= f.label :project_name, "ชื่อโครงการ/กิจกรรม" %><br />
    <%= f.text_field :project_name %>
  </div>
  <div class="field">
    <%= f.label :target_name, "กลุ่มเป้าหมาย" %><br />
    <%= f.text_field :target_name %>
  </div>
  <div class="field">
    <%= f.label :activity_type, "ลักษณะกิจกรรม" %><br />
     <%= f.select(:activity_type, ApplicationController::ACTIVITY_TYPE , {:prompt => 'เลือกประเภท'}) %>
    <!-- <%= f.text_field :activity_type %> -->
  </div>
  <div class="field">
    <%= f.label :target_no_2554, "จำนวนกลุ่มเป้าหมาย ปี 2554" %><br />
    <%= f.text_field :target_no_2554 %>
  </div>
  <div class="field">
    <%= f.label :team_type, "ประเภททีมงานที่ต้องใช้" %><br />
    <%= f.select(:team_type, ApplicationController::TEAM_TYPE , {:prompt => 'เลือกประเภท'}) %>
    <!-- <%= f.text_field :team_type %> -->
  </div>
  <div class="field">
    <%= f.label :team_no, "จำนวนทีมงานที่ต้องใช้" %><br />
    <%= f.text_field :team_no %>
  </div>
  <div class="field">
    <%= f.label :comment, "หมายเหตุ" %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

