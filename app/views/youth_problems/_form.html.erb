<%= form_for(@youth_problem) do |f| %>
  <% if @youth_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@youth_problem.errors.count, "error") %> prohibited this youth_problem from being saved:</h2>

      <ul>
      <% @youth_problem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
