<%= simple_form_for(@leader) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :title %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>

