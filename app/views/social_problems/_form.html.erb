<%= form_for(@social_problem) do |f| %>
  <% if @social_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@social_problem.errors.count, "error") %> prohibited this social_problem from being saved:</h2>

      <ul>
      <% @social_problem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :rows => 5 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
