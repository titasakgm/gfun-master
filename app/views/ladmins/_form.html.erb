<%= form_for(@ladmin) do |f| %>
  <% if @ladmin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ladmin.errors.count, "error") %> prohibited this ladmin from being saved:</h2>

      <ul>
      <% @ladmin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
