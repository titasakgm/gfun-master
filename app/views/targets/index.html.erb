<h1>Listing targets</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Code</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @targets.each do |target| %>
  <tr>
    <td><%= target.title %></td>
    <td><%= target.code %></td>
    <td><%= target.description %></td>
    <td><%= link_to 'แสดง', target %></td>
    <td><%= link_to 'แก้ไข', edit_target_path(target) %></td>
    <td><%= link_to 'ลบ', target, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Target', new_target_path %>

