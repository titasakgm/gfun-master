<%= form_for(@community) do |f| %>
  <% if @community.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@community.errors.count, "error") %> prohibited this community from being saved:</h2>

      <ul>
      <% @community.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ชื่อชุมชน %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :รายละเอียด %><br />
    <%= f.text_area :desciption %>
  </div>
  <div class="field">
    <%= f.label :ที่อยู่ %><br />
    <%= f.text_area :address %>
  </div>
  <div class="actions">
    <%= f.submit  "บันทึก" %>
  </div>
<% end %>

