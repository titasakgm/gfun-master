<h1>Site#test</h1>
<p>Find me in app/views/site/test.html.erb</p>
<% form_tag "/test", :remote=>true, :id => 'myform' do %>
        Enter the public url:<%= text_field_tag 'url' ,'', :size => 80 %>
        <%= submit_tag "Find" %>
        <% end %>
<%= link_to "new comment", "test",
:remote => true, :id => 'new_comment_link' %>
		<!-- 1. Add these JavaScript inclusions in the head of your page -->
		<%= javascript_include_tag "high-chart/highcharts" %>
		<!-- <script type="text/javascript" src="../js/highcharts.js"></script> -->

		<!-- 1a) Optional: add a theme file -->

		<!--	<script type="text/javascript" src="../js/themes/gray.js"></script> -->
    <%= javascript_include_tag "high-chart/themes/gray" %>
		<!-- 1b) Optional: the exporting module -->


		<!-- 2. Add the JavaScript to initialize the chart on document ready -->
		<script type="text/javascript">

			var chart;
			$(document).ready(function() {
				chart = new Highcharts.Chart({
					chart: {
						renderTo: 'container',
						defaultSeriesType: 'bar'
					},
					title: {
						text: 'Historic World Population by Region'
					},
					subtitle: {
						text: 'Source: Wikipedia.org'
					},
					xAxis: {
						categories: ['Africa', 'America', 'Asia', 'Europe', 'Oceania'],
						title: {
							text: null
						}
					},
					yAxis: {
						min: 0,
						title: {
							text: 'Population (millions)',
							align: 'high'
						}
					},
					tooltip: {
						formatter: function() {
							return ''+
								 this.series.name +': '+ this.y +' millions';
						}
					},
					plotOptions: {
						bar: {
							dataLabels: {
								enabled: true
							}
						}
					},
					legend: {
						layout: 'vertical',
						align: 'right',
						verticalAlign: 'top',
						x: -10,
						y: 30,
						floating: true,
						borderWidth: 1,
						backgroundColor: '#FFFFFF',
						shadow: true
					},
					credits: {
						enabled: false
					},
				        series: [{
						name: 'Year 1800',
						data: [107, 31, 635, 203, 2]
					}, {
						name: 'Year 1900',
						data: [133, 156, 947, 408, 6]
					}, {
						name: 'Year 2008',
						data: [973, 914, 4054, 732, 34]
					}]
				});


			});

		</script>
		<div id="container" style="width: 400px; height: 300px; margin: 0 auto"></div>

