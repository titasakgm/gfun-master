<h1>Listing helps</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @helps.each do |help| %>
  <tr>
    <td><%= help.title %></td>
    <td><%= link_to 'แสดง', help %></td>
    <td><%= link_to 'แก้ไข', edit_help_path(help) %></td>
    <td><%= link_to 'ลบ', help, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Help', new_help_path %>

