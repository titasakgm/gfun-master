<h1>Listing lr20s</h1>
<h3>ปัญหาเชิงพฤติกรรมในพื้นที่</h3>
<table>
  <tr>

    <th>ปัญหาเชิงพฤติกรรม</th>
    <th>สถานะ</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lr20s.each do |lr20| %>
  <tr>
    <!-- <td><%= lr20.user_id %></td> -->
    <td><%= lr20.behavior_problem %></td>
    <td><%= ApplicationController::HAVE_STATUS.index(lr20.status) %></td>
    <td><%= link_to 'แสดง', lr20 %></td>
    <td><%= link_to 'แก้ไข', edit_lr20_path(lr20) %></td>
    <td><%= link_to 'ลบ', lr20, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Lr20', new_lr20_path %>

