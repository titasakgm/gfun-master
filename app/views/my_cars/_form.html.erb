<%= form_for(@my_car) do |f| %>
  <% if @my_car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_car.errors.count, "error") %> prohibited this my_car from being saved:</h2>

      <ul>
      <% @my_car.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
